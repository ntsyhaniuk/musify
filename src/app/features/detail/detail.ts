import { Component, ChangeDetectionStrategy } from '@angular/core';

/**
 * Album / artist / playlist details — Phase 2 (features-ui).
 */
@Component({
  selector: 'app-detail',
  imports: [],
  templateUrl: './detail.html',
  styleUrl: './detail.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Detail {}
