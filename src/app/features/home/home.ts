import { Component, ChangeDetectionStrategy } from '@angular/core';

/**
 * Library hub home — Phase 2 (features-ui / spotify-data).
 */
@Component({
  selector: 'app-home',
  imports: [],
  templateUrl: './home.html',
  styleUrl: './home.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Home {}
